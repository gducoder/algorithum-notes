<nav class="navbar navbar-inverse">
  <div class="container-fluid">
    <div class="navbar-header">
      <a class="navbar-brand" href="#">Dispatch</a>
    </div>
    <% if user_signed_in? && current_user.user_type == "Admin"%>
    <ul class="nav navbar-nav">

      <li class="active"><%= link_to 'Home', jobs_path, :class => 'navbar-link'%></li>
	 </ul>
      <% elsif  user_signed_in? && current_user.user_type == "User"%>
      <ul class="nav navbar-nav">

        <li class="active"><%= link_to 'Home', user_path(current_user.id), :class => 'navbar-link'%></li>
        <li><%= link_to 'Notifications', notifications_path, :class => 'navbar-link'%></li>
      </ul>
      <% end %>
      <p class="navbar-text pull-right">

        <% if user_signed_in? && current_user.user_type == "Admin"%>
          <%= link_to 'Drivers', user_index_path, :class => 'navbar-link' %> |
          Logged in as <strong><%= current_user.email %> |ADMIN| </strong>
          <%= link_to 'Edit profile', edit_user_registration_path, :class => 'navbar-link' %> |
          <%= link_to "Logout", destroy_user_session_path, method: :delete, :class => 'navbar-link'  %>

          <% elsif user_signed_in? %>

            Logged in as <strong><%= current_user.email %></strong>
            <%= link_to 'Edit profile', edit_user_registration_path, :class => 'navbar-link' %> |
            <%= link_to "Logout", destroy_user_session_path, method: :delete, :class => 'navbar-link'  %>

	<% else %>

		      <%= link_to "Sign up", new_user_registration_path, :class => 'navbar-link'  %> |
		      <%= link_to "Login", new_user_session_path, :class => 'navbar-link'  %>
		      <% end %>
		</p>
    
  </div>
</nav>
  